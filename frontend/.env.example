# Copie para .env.local e ajuste
# URL do backend Flask
VITE_API_URL=http://localhost:8000
# Chave do Google Maps (Maps JavaScript API). Se vazio, o mapa usa
# Leaflet + OpenStreetMap automaticamente.
VITE_GOOGLE_MAPS_API_KEY=
